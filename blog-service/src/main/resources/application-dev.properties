# Development profile - relaxed security, verbose logging

# JWT (use different secret in production!)
jwt.secret=dev_secret_404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
jwt.expiration=36000000
jwt.cookie.secure=false

# SQL Logging (helpful for debugging)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Security (relaxed for development)
cors.allowed-origins=http://localhost:3000,http://localhost:3001

# Logging
logging.level.com.example.backend=DEBUG

# Connection pool (smaller for dev)
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=2